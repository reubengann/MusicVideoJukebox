<UserControl x:Class="MusicVideoJukebox.Views.AnalyzeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicVideoJukebox.Views"
             xmlns:vms="clr-namespace:MusicVideoJukebox.Core.ViewModels;assembly=MusicVideoJukebox.Core"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vms:AnalyzeViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="480" d:DesignWidth="850" Background="{DynamicResource MahApps.Brushes.ThemeBackground}">
    <Viewbox>
        <Grid Width="1050" Height="580">
            <Grid.RowDefinitions>
                <RowDefinition Height="520" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DataGrid ItemsSource="{Binding AnalysisResults}" 
                      AutoGenerateColumns="False" 
                      SelectionMode="Extended" 
                      SelectionUnit="FullRow" 
                      IsReadOnly="True"
                      Margin="5"
                      MaxHeight="500" BorderThickness="1"
                      PreviewMouseLeftButtonDown="DataGrid_PreviewMouseLeftButtonDown"
                      PreviewTextInput="DataGrid_PreviewTextInput"
                      >
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MahApps.Styles.DataGridRow}">
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay, NotifyOnTargetUpdated=True}" />
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Filename" Binding="{Binding Filename}" Width="280"/>
                    <DataGridTextColumn Header="VCodec" Binding="{Binding VideoCodec}" Width="100"/>
                    <DataGridTextColumn Header="ACodec" Binding="{Binding AudioCodec}" Width="100"/>
                    <DataGridTextColumn Header="Width" Binding="{Binding VideoWidth}" Width="70"/>
                    <DataGridTextColumn Header="Height" Binding="{Binding VideoHeight}" Width="70"/>
                    <DataGridTextColumn Header="Loudness" Binding="{Binding LUFS}" Width="100"/>
                    <DataGridTextColumn Header="" Binding="{Binding Warning}" Width="150"/>
                </DataGrid.Columns>
                <!--<DataGrid.ContextMenu>
                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="Normalize" Command="{Binding NormalizeTrackCommand}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>-->
            </DataGrid>
            <mah:ProgressRing Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <UniformGrid Margin="5" Rows="1" Width="500">
                    <Button Command="{Binding CancelOperationsCommand}">Cancel</Button>
                    <Button Command="{Binding NormalizeSelectedCommand}" Margin="5,0,0,0">Normalize Selected</Button>
                    <!--<Button Command="{Binding AnalyzeSelectedCommand}" Margin="5,0,0,0">Analyze Selected</Button>-->
                    <Button Command="{Binding AnalyzeAllCommand}" Margin="5,0,0,0">Normalize All</Button>
                </UniformGrid>
                <TextBlock Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" Text="{Binding OperationText}"/>
            </Grid>
        </Grid>
    </Viewbox>
</UserControl>
