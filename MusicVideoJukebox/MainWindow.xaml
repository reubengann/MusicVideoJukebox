<Window x:Class="MusicVideoJukebox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicVideoJukebox"
        mc:Ignorable="d"
        Title="MainWindow" Height="750" Width="1000"
        MouseDoubleClick="Window_MouseDoubleClick">
    <Grid Background="Black">
        <MediaElement x:Name="player" LoadedBehavior="Manual" UnloadedBehavior="Manual">
            
        </MediaElement>
        
        <Grid VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Width="50" Content="Play" Command="{Binding PlayCommand}"/>
                <Button Grid.Column="1" Width="50" Content="Pause" Command="{Binding PauseCommand}"/>
                <Button Grid.Column="2" Width="50" Content="Stop" Command="{Binding StopCommand}"/>
            </StackPanel>

            <Slider Grid.Column="4" VerticalAlignment="Center" Maximum="1" Minimum="0" SmallChange="0.01" LargeChange="0.1" Value="{Binding Volume}"/>
            <Slider VerticalAlignment="Center" Grid.Row="1" Grid.ColumnSpan="5" Maximum="{Binding VideoLengthSeconds}" Value="{Binding VideoPositionTime}"
        PreviewMouseLeftButtonDown="Slider_PreviewMouseLeftButtonDown"
        PreviewMouseLeftButtonUp="Slider_PreviewMouseLeftButtonUp"
        IsMoveToPointEnabled="True"
        />
        </Grid>
        
    </Grid>
</Window>
