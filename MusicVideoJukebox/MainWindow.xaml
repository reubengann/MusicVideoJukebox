<Window x:Class="MusicVideoJukebox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicVideoJukebox"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <MediaElement Grid.Row="0" x:Name="player" LoadedBehavior="Manual" UnloadedBehavior="Manual">
            
        </MediaElement>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Width="50" Content="Play" Command="{Binding PlayCommand}"/>
                <Button Grid.Column="1" Width="50" Content="Pause" Command="{Binding PauseCommand}"/>
                <Button Grid.Column="2" Width="50" Content="Stop" Command="{Binding StopCommand}"/>
            </StackPanel>

            <Slider Grid.Column="4" VerticalAlignment="Center" Maximum="1" Minimum="0" SmallChange="0.01"  LargeChange="0.1"/>
        </Grid>
        <Slider VerticalAlignment="Center" Grid.Row="2" Maximum="{Binding VideoLengthSeconds}" Value="{Binding VideoPositionTime}"/>
    </Grid>
</Window>
